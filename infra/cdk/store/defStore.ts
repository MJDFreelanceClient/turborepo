import {Store} from "@tanstack/store";

export const lambdaDefStore = new Store<Record<string, any>>({

});

export const layerDefStore = new Store<Record<string, any>>({

});

export const tableDefStore = new Store<Record<string, any>>({

});