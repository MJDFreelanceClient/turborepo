import {Store} from "@tanstack/store";

export const lambdaConfigStore = new Store<Record<string, any>>({

});

export const layerConfigStore = new Store<Record<string, any>>({

});

export const globalConfigStore = new Store<Record<string, any>>({

});